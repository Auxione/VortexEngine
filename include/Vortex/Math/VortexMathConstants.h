#pragma once

namespace Vortex::Math {
	constexpr static float PI = 3.14159265359f;
	constexpr static float DegToRad = (PI * 2.0f) / 360.0f;
	constexpr static float RadToDeg = 360.0f / (PI * 2.0f);
	constexpr static float Epsilon = 0.00001f;
}